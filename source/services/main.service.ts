export default class MainService {

    static $inject: string[] = [
        '$http'
    ];

    constructor(
        private $http: angular.IHttpService
    ) {

    }

    /**
     * mainMethod
     */
    public mainMethod() {
        
    }

}